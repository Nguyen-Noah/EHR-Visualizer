const {DatabaseSingleton} = require("../../connectionSingleton");

module.exports.search = async(args, context) => {
    
    throw new Error("Unable to locate patients");
};

module.exports.searchById = async(args, context) => {
    throw new Error("Unable to locate patients");
}

